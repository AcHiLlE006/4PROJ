import { IncidentArchivedEntity } from './incident_archived.entity';

describe('IncidentArchivedEntity', () => {
  it('should be defined', () => {
    expect(new IncidentArchivedEntity()).toBeDefined();
  });
});
